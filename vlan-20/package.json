{
  "name": "revenant-vlan20-faq-service",
  "version": "1.0.0",
  "description": "REVENANT v3.2 — VLAN 20 FAQ Service. Stateless synchronous gRPC pipe answering non-monetary inquiries via the AI Cognition Service. No Temporal workflows, no database, by deliberate design — see server.ts header.",
  "type": "module",
  "main": "dist/server.js",
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.10.9",
    "@grpc/proto-loader": "^0.7.13",
    "zod": "^3.23.8",
    "uuid": "^9.0.1",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "@types/node": "^20.14.15",
    "@types/uuid": "^9.0.8"
  }
}